# Amigo Invisible · Fulbito
Next.js + Supabase. Core integration: email OTP auth, events, participants, preferences, protected assignments and server-side draw RPC.

## Local
1. `npm install`
2. Copy `.env.example` to `.env.local` and set `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`.
3. `npm run dev`
